# Cloudflare Pages Configuration for Static Site
# 静态网站的Cloudflare Pages配置

# 项目名称
name = "tour-business-in-china"

# 兼容性日期
compatibility_date = "2026-01-23"

# 移除[site]配置块，因为对于静态网站，我们不需要这个配置
# Cloudflare Pages会自动识别dist目录中的静态文件