# Cloudflare Pages Configuration
# 这个文件配置Cloudflare Pages的构建和部署设置

# 项目名称
name = "tour-business-in-china"

# Cloudflare Pages不需要main入口文件和compatibility_date

# 构建配置已经在package.json中定义，这里不需要重复定义

# 移除了不支持的字段，Cloudflare Pages会自动使用package.json中的配置